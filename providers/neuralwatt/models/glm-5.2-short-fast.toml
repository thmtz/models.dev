# Non-thinking tier of the glm-5.2 200K short pool (reasoning_effort = none
# injected server-side).
# Host deltas vs the lab model: no structured output (json_mode = false on the
# live catalog), 200K context with a 32K output cap.
base_model = "zhipuai/glm-5.2"
name = "GLM-5.2 (short, fast)"
reasoning = false
structured_output = false

[cost]
input = 1.45
output = 4.5
cache_read = 0.145

[limit]
context = 199_984
output = 32_000
