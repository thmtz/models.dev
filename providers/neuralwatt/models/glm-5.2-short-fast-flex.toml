# Non-thinking flex tier of the glm-5.2 200K short pool (reasoning_effort =
# none injected server-side). Async/queued processing at a 0.65 pricing
# multiplier (35% off) vs glm-5.2-short-fast. Requires stream = true
# (non-streaming requests fall through to the standard tier).
# Host deltas vs the lab model: no structured output (json_mode = false on the
# live catalog), 200K context with a 32K output cap.
base_model = "zhipuai/glm-5.2"
name = "GLM-5.2 (short, fast, flex)"
reasoning = false
structured_output = false

[cost]
input = 0.9425
output = 2.925
cache_read = 0.09425

[limit]
context = 199_984
output = 32_000
