# Non-thinking tier of glm-5.2 (reasoning_effort = none injected server-side).
# Host deltas vs the lab model: no structured output (json_mode = false on the
# live catalog), a slightly larger served context (1_048_560), and no separate
# output cap (output = context).
base_model = "zhipuai/glm-5.2"
name = "GLM-5.2 (fast)"
reasoning = false
structured_output = false

[cost]
input = 1.45
output = 4.5
cache_read = 0.145

[limit]
context = 1_048_560
output = 1_048_560
